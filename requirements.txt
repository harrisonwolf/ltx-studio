# LTX Studio dependencies.
#
# Targets Python 3.10+ and open-weights video-diffusion backends running locally.
# Model weights are NOT listed here — they are fetched on first run via huggingface_hub.

# ── TUI + orchestration (the studio itself) ──
textual>=0.86            # textual.theme.Theme (the theme registry) landed in 0.86
rich
numpy
Pillow

# ── diffusion backends: LTX-Video 2B, Wan 2.1-VACE ──
# NOTE: on Blackwell / RTX 50-series (sm_120) install a CUDA 12.8 torch build first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.4
diffusers>=0.34
transformers>=4.44
accelerate
safetensors
sentencepiece
huggingface_hub
imageio                 # diffusers.export_to_video writes mp4 through imageio...
imageio-ffmpeg          # ...with this ffmpeg backend (the studio also uses its ffmpeg for thumbnails)

# ── optional: Qwen3-VL sidecars (director-mode prompt rewriting, CONSULT) ──
# These run in a SEPARATE venv (LTX_DIRECTOR_PY, see README) that also needs transformers>=4.57
# and bitsandbytes; listed here so a single-env setup can import them.
qwen-vl-utils

# ── dev ──
# pyflakes              # tests/test_lint.py
# pandas                # experiment_log.to_dataframe() for analysis
